🏥 Patient Management System deployed successfully!

Get the application URL:
  kubectl get svc -n {{ .Values.namespace }} patient-service

Port forward to access locally:
  kubectl port-forward -n {{ .Values.namespace }} svc/patient-service 9090:80

Default login:
  Username: admin
  Password: Admin@1234

Services running:
  - Patient App    (port {{ .Values.patientApp.port }})
  - Auth Service   (port {{ .Values.authService.port }})
  - PostgreSQL     (port {{ .Values.postgres.port }})
